{% extends 'base.html.twig' %}

{% block title %}TemplatePage{% endblock %}

{% block body %}
    <h1>TemplatePage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ template_page.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ template_page.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ template_page.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_template_page_index') }}">back to list</a>

    <a href="{{ path('app_template_page_edit', {'id': template_page.id}) }}">edit</a>

    {{ include('template_page/_delete_form.html.twig') }}
{% endblock %}
