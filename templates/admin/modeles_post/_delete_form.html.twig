<form method="post" action="{{ path('app_modeles_post_delete', {'id': modeles_post.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ modeles_post.id) }}">
    <button class="btn">Delete</button>
</form>
