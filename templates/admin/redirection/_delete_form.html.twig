<form method="post" action="{{ path('app_redirection_delete', {'id': redirection.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ redirection.id) }}">
    <button class="btn">Delete</button>
</form>
