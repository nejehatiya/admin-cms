<form method="post" action="{{ path('app_post_meta_fields_delete', {'id': post_meta_field.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post_meta_field.id) }}">
    <button class="btn">Delete</button>
</form>
