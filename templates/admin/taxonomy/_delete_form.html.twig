<form method="post" action="{{ path('app_taxonomy_delete', {'id': taxonomy.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ taxonomy.id) }}">
    <button class="btn">Delete</button>
</form>
